# Connector Configuration for AnythingLLM
# Add these to your .env file to enable data connectors

# ============================================
# NANGO CONFIGURATION (Recommended for OAuth)
# ============================================
# Sign up for free at https://www.nango.dev or self-host with:
# docker run -p 3003:3003 nangohq/nango

NANGO_SECRET_KEY=your-nango-secret-key-here
NANGO_HOST=http://localhost:3003
# NANGO_PUBLIC_KEY=your-nango-public-key # Optional, for frontend

# ============================================
# DIRECT API CREDENTIALS (if not using Nango)
# ============================================
# Add these if you want to use API keys directly without OAuth

# Shopify
# SHOPIFY_API_KEY=your-shopify-api-key
# SHOPIFY_API_SECRET=your-shopify-api-secret

# Google (requires OAuth setup in Google Cloud Console)
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# Stripe
# STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key

# GitHub
# GITHUB_PERSONAL_ACCESS_TOKEN=ghp_your-github-token

# ============================================
# MCP SERVER PACKAGES (Optional)
# ============================================
# These MCP servers will be installed automatically if not present
# You can also install them globally with npm install -g

# MCP_SHOPIFY_PACKAGE=@shopify/mcp-server
# MCP_GOOGLE_PACKAGE=google-workspace-mcp
# MCP_GITHUB_PACKAGE=@modelcontextprotocol/server-github

# ============================================
# SYNC CONFIGURATION
# ============================================
# How often to sync data from connected sources (in minutes)
# CONNECTOR_SYNC_INTERVAL=30

# Maximum number of records to sync per provider
# CONNECTOR_SYNC_LIMIT=1000